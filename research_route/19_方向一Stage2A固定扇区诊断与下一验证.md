# 方向一 Stage 2-A 固定扇区诊断与下一验证

更新日期：2026-09-15
状态：F1 真值峰—错误峰及单径/三径分解已完成；当前单配置状态存在约 1.8–2.2°的高相关角度竞争峰，多径贪婪选径为次要问题，正式比较仍暂停

## 1. 本轮问题与边界

本轮运行 `stage2a_bounded_20260910_v1` 使用实验开始前固定的已知入射扇区字典，目的是检查修正后的三维 Grid OMP 与 Yang-style OG-OLS 能否在小规模、真值被字典覆盖的条件下恢复三径。它不改变 [[17_方向一Stage2统一二维基准|Stage 2 统一二维场景]]，也不把方位 10–30°、俯仰 −9–9°设为最终研究范围。

固定条件为：

- 300 GHz 载频、30 GHz 带宽、128 个子载波；
- 8×160 二维接收 DMA，共 1280 个阵元；微带内间距 $0.2\lambda_c$，微带间距 $0.5\lambda_c$；
- 精确球面 LoS+两反射径、用户给定分子吸收表、Lorentzian—波导频变 DMA 响应；
- F1 为 1 配置×128 子载波，F2 为 8 配置×16 子载波，两者均占 128 个导频 RE；
- 5 个随机种子各生成 1 个独立三径信道；阵列输入参考 SNR 为 55 dB；
- 字典在信道生成前固定为距离 1.80–2.45 m、方位 10–30°、俯仰 −9–9°，步长为 1 cm/2°/3°，共 5,082 个候选；
- Oracle geometry+LS、3-D Grid OMP 和 Yang-style 3-D OG-OLS 在同一协议、场景和噪声下配对。

运行完成 30/30 行，数据无重复或非有限主指标。同协议内三方法确实共享观测，Oracle 几何完全匹配，Yang 每一径均按“选支持—连续细化—更新残差”执行且残差不增加。两套 `complex64` 字典共 79.4 MiB；F1/F2 构建时间分别为 39.29 s 和 5.80 s，总运行时间为 55.99 s。

## 2. 结果与预期差异

| 协议 | 方法 | CSI NMSE | 三径全部成功 | 中位波束损失 | 平均净谱效率 |
|---|---|---:|---:|---:|---:|
| F1 | Grid OMP | 1.804 dB | 0/5 | 9.085 dB | 9.038 bit/s/Hz |
| F1 | Yang OG-OLS | 2.267 dB | 0/5 | 8.920 dB | 8.618 bit/s/Hz |
| F1 | Oracle | −26.444 dB | 5/5 | 0.001 dB | 11.579 bit/s/Hz |
| F2 | Grid OMP | −4.798 dB | 0/5 | 0.943 dB | 10.326 bit/s/Hz |
| F2 | Yang OG-OLS | −8.242 dB | 0/5 | 1.115 dB | 10.244 bit/s/Hz |
| F2 | Oracle | −28.960 dB | 5/5 | 0.001 dB | 10.556 bit/s/Hz |

预期是 OMP 和 OG-OLS 都能给出基本有效的三径估计，OG-OLS 再以连续细化降低离网格误差。实际结果只满足“程序与精确几何链路能运行”，没有满足“可部署方法能恢复三径”：两个协议中 OMP、OG-OLS 的三径成功率均为 0%。因此不能把当前数值写成可接受的方法差距，也不能据此开始正式排名。

F1 中 Yang 相对 OMP 只在 1/5 场景降低 NMSE，配对中位差为 $+0.454$ dB，未形成预期的稳定细化收益。F2 中 Yang 在 4/5 场景降低 NMSE，配对平均差为 $-3.687$ dB、中位差为 $-1.559$ dB，但路径恢复仍全部失败，通信端点也没有随 CSI NMSE 同步改善。

## 3. 55 dB 后的初步故障定位

### 3.1 F1 主要表现为空间角度歧义

F1 的 Yang 距离 RMSE 中位数仅约 2.6 mm、俯仰 RMSE 中位数约 0.81°，但方位 RMSE 中位数约 7.82°；15 条估计路径中有 5 条落在方位搜索下边界 10°。真值支持已经由预检查确认位于固定字典内，因此当前主问题不是“字典没有覆盖真值”，而是单配置 F1 状态在约 0 dB 实际 DMA 输出 SNR 下没有提供足够稳定的方位区分，或噪声使粗支持先落入错误角度盆地。

### 3.2 F2 主要表现为稀疏频点导致的距离混叠

F2 的 Yang 距离 RMSE 中位数约 0.222 m，15 条路径的距离误差均超过 3 cm。16 个频点索引为 `[0,8,17,25,34,42,51,59,68,76,85,93,102,110,119,127]`，相邻间隔为 8 或 9 个子载波，即 1.875 或 2.109 GHz。这种近似梳状频率采样会形成约 0.15–0.16 m 的重复距离相位模式；多数误差接近该周期的整数倍，符合距离栅瓣而非单纯网格量化误差。

### 3.3 当前 CSI NMSE 口径存在同点评估问题

代码把 `channel_nmse_subcarrier_count = 16` 转为上述 16 个等间隔频点。它们恰好就是 F2 的全部观测频点，因此 F2 的 −8.242 dB 只说明对导频频点的拟合，不能证明其余 112 个子载波上的宽带 CSI 恢复。F1 也只在其 128 个已观测频点中的同一 16 点子集上评价。正式比较前必须把导频域、全 128 子载波和 F2 的 112 个未观测子载波分开报告。

### 3.4 已排除与尚未排除

Oracle 在 F1/F2 分别达到 −26.44/−28.96 dB 且三径全部成功，说明信道生成、DMA 前向算子、噪声白化、增益拟合、CSI 回构和路径匹配链路没有整体失效。Yang 各阶段残差单调下降，也说明上一轮“先选满支持再细化”的实现顺序错误已修复。

这两项检查不能证明 F1/F2 可辨识。Yang 每条路径只运行 2 次局部迭代，三个阶段都达到迭代上限，但末阶段残差降幅已经很小；单纯增加迭代次数预计不能把错误角度盆地或距离栅瓣变成正确支持。

## 4. 下一次最小实验

下一次仍使用同一固定小字典、同一 5 个信道种子、同一 F1/F2 状态和同一估计器，不扩大角度、距离、阵列或方法集合，也不启动 100 场景正式配置。

1. 修正评价口径，同时保存 `pilot_nmse`、`fullband_128_nmse` 和 F2 的 `heldout_112_nmse`。原 `channel_nmse` 暂保留为兼容字段，但不能再单独承担结论。
2. 只增量运行 65 dB 输入参考点，并复用当前 5 个场景及标准噪声实现。它用于判断 F1 方位失败是否主要由约 0 dB 输出 SNR造成，不形成 SNR 曲线。
3. 不改变字典和 F2 导频位置，以便先把失败原因归因清楚；导频重排属于随后的协议修正，不与本次 SNR 检查混在一起。

判据为：

- 若 F1 在 65 dB 下角度与三径成功率明显恢复，保留 F1 状态并在正式 SNR 区间评价；若仍稳定命中错误方位或边界，停止扩大 Monte Carlo，转而设计具有明确角频编码的 F1 DMA 状态，并把 Deshpande 类机制作为状态/直接波束适配对象。
- 若 F2 的导频域 NMSE 良好而全带和未观测频点 NMSE 明显恶化，支持固定 16 点梳状导频的距离混叠解释；导频排布的因果判断仍需下一阶段预先固定非均匀导频的对照。若三种 NMSE 都差，再检查 F2 状态与三维稀疏估计的联合可辨识性。
- 只有 OMP 与 OG-OLS 在固定扇区内都能产生基本有效、物理可解释的估计后，才运行 `direction1_stage2a_classical.toml` 的 100 场景正式比较，并接入 Deshpande 与接收侧物理感知张量方法。

## 5. 55 dB 阶段文件索引

- 运行入口：`scripts/run_direction1_stage2a.py`
- 快速分析与绘图：`scripts/analyze_direction1_stage2a.py`
- 固定扇区配置：`configs/direction1_stage2a_bounded_diagnostic.toml`
- 正式配置：`configs/direction1_stage2a_classical.toml`
- 最新运行：`runs/stage2a_bounded_20260910_v1/`
- 紧凑分析：`runs/stage2a_bounded_20260910_v1/analysis/analysis_compact.json`
- 人工报告：`runs/stage2a_bounded_20260910_v1/analysis/analysis_report.md`
- 探索性概览：`runs/stage2a_bounded_20260910_v1/analysis/eda_profile.json`

Stage 2-A 的完整模型与公共比较口径见 [[18_方向一Stage2A三维传统参照]]；已确认的范围和下一步约束见 [[99_研究决策记录]]。

## 6. 本轮实现与运行交付

独立配置为 `configs/direction1_stage2a_bounded_65db_diagnostic.toml`。与旧运行解析配置核对后，差异仅为输入参考 SNR 55→65 dB、实验名称及分析主 SNR 标签。字典仍为 5,082 点，5 个信道种子、真值范围、F1/F2 状态和导频、Yang 的 2 次迭代全部不变。仍标记 `diagnostic_only`，预期 30 行，由用户运行。

每个频率域 $D$ 使用阵元 CSI 的能量比：

$$
\operatorname{NMSE}_D=\frac{\sum_{k\in D}\lVert\widehat{H}[k]-H[k]\rVert_F^2}{\sum_{k\in D}\lVert H[k]\rVert_F^2}.
$$

导频点按唯一频率计数，不随配置重复计权。它与 DMA 合并后的 `observation_nmse` 是不同指标。每个新字段均保存 `_linear`、`_db` 和 `_subcarrier_count`：

| 字段前缀 | F1 | F2 |
|---|---|---|
| `pilot_nmse` | 128 点 | 原 16 个导频点 |
| `fullband_128_nmse` | 全 128 点 | 全 128 点 |
| `heldout_112_nmse` | 不适用：计数 0，CSV 留空、JSON 为 null | 导频补集 112 点 |
| `channel_nmse` | 原 16 点子集，保持旧语义 | 原 16 点导频，保持旧语义 |

全带 CSI 只重构一次，再取各域求误差。新增评价计算在估计器计时之外；不影响估计观测、随机数或优化过程。运行汇总保存实际导频与未观测索引。旧 55 dB 原始表未保存估计复增益，因此本轮不补算、覆盖旧运行，也不把新 65 dB NMSE 冒充旧 55 dB 指标。

运行命令，工作目录为项目根目录：

```powershell
& 'D:\Program Files\Miniconda\Scripts\conda.exe' run --no-capture-output -n ris_td3 python scripts\run_direction1_stage2a.py --config configs\direction1_stage2a_bounded_65db_diagnostic.toml --run-id stage2a_bounded_65db_20260910_v1
```

预期产物位于 `runs/stage2a_bounded_65db_20260910_v1/`：

- `metrics_raw.csv`：30 行原始结果，含三个新评价域和旧兼容字段。
- `metrics_summary.json`：评价域定义、实际频点索引、资源与计时。
- `analysis/summary_table.csv`：旧列及新增分域均值、场景 bootstrap 95% 区间；均值先在线性域聚合再转 dB。
- `analysis/domain_summary_table.csv`、`analysis/domain_paired_method_effects.csv`：分域快速汇总及同协议内相对 OMP 的配对 dB 差值。Oracle 单列为几何参照。
- `analysis/analysis_compact.json`、`analysis/analysis_report.md`：F1 55→65 dB 配对判断及 F2 三域判断。自动读取同级旧运行 `stage2a_bounded_20260910_v1`，保留其原始 CSV 哈希；配置不一致或真值不匹配时不作 SNR 归因。
- `analysis/figures/`：三张既有图；NMSE 图和时间权衡图采用全 128 点指标并明确标注。旧 `primary_screen` 与原配对字段保留兼容语义，不能用作宽带结论。
- `results/direction1_stage2a/`：新运行的汇总与紧凑分析副本。

快速分析采用以下预先记录的描述性阈值，不调估计器参数，也不作为显著性检验或正式论文门槛：

1. **F1 改善**：OMP、Yang 各自至少 4/5 场景方位 RMSE 比 55 dB 下降，且配对中位差小于 0。**方位恢复**：至少 4/5 场景的全部三径方位误差在原 3° 容差内。另列三径联合成功数、方位上下边界命中与逐场景差值，区分“改善但未恢复”。
2. **F2 分域失配**：每场景导频 NMSE≤−5 dB，且全带或未观测域相对导频恶化≥6 dB；至少 4/5 场景符合时标记“导频域好、全带或未观测域差”。同时保存三个逐场景 NMSE 和域间中位差，避免仅凭均值判断。
3. 阈值未通过时报告未通过，不能把“未达到该现象判据”写成没有混叠。F1/F2 未恢复时继续暂停正式运行。状态重设计、导频重排及新文献方法均留待读取新结果后的阶段。

必要检查入口：

```powershell
& 'D:\Program Files\Miniconda\Scripts\conda.exe' run -n ris_td3 python -B scripts\check_stage2a_domains.py
```

本轮检查已通过：静态解析、非均匀真值能量下的分域归一化、F1 空未观测域、5 个旧场景几何一致、55/65 dB 标准噪声缩放、单场景双协议 Oracle 分域接线、旧格式重分析兼容、合成数据的正负判断与配置不匹配拒绝。报告测试产物仅在临时目录生成并自动清理，历史运行全部文件哈希保持不变。没有构建字典、调用 OMP/Yang、启动 30 行完整诊断或 100 场景正式运行。

## 7. 65 dB 运行结果与判断修订

本节为第 6 节交付之后的结果。用户已完成 `stage2a_bounded_65db_20260910_v1`，30/30 行、数据配对与分域检查通过；第 6 节“尚未运行”仅描述当时交付状态。

| 协议 | 方法 | 导频域 NMSE/dB | 全带 NMSE/dB | 未观测域 NMSE/dB | 三径成功 |
|---|---|---:|---:|---:|---:|
| F1 | OMP | 1.705 | 1.705 | 不适用 | 0/5 |
| F1 | Yang | 2.273 | 2.273 | 不适用 | 0/5 |
| F1 | Oracle | −36.473 | −36.473 | 不适用 | 5/5 |
| F2 | OMP | −4.901 | 2.408 | 2.885 | 0/5 |
| F2 | Yang | −8.707 | 3.124 | 3.668 | 0/5 |
| F2 | Oracle | −38.960 | −38.968 | −38.969 | 5/5 |

表中均先取线性 NMSE 均值再转 dB。65 dB 时实际输出 SNR 均值为 F1 9.96 dB、F2 11.98 dB。Oracle 随输入 SNR 增加约改善 10 dB，OMP/Yang 仍未恢复。F1 Yang 方位 RMSE 中位数仍约 7.82°，下边界命中仍为 5/15；自动“改善”标签仅由约 −0.004° 的配对中位差触发，不能解释为实质改善。保留原报告及其预设阈值，不事后改写结果。

在不更改配置的单径算子检查中，第一个旧场景真方位为 21.43°，固定真实距离和俯仰，F1 错误 14° 模板的归一化相关度平方为 0.869，高于相邻 20°/22° 网格的 0.715/0.644；精确真值为 1。F2 同一单径距离偏移 0.1505 m 后，相关度平方仍为 0.966。这些数值支持 F1 网格与观测响应失配、F2 距离伪峰的解释，但不证明连续参数在无噪声下全局不可辨识。探索性记录见 `results/direction1_stage2a/stage2a_65db_operator_review.json`；该文件不是正式 Monte Carlo。

此前将下一步直接指向状态或导频修改仍过早。应先按用户确认的 D-022 检查现有字典上的无噪声恢复能力，结果如下。

## 8. 无噪声网格内单径与三径验收

运行 `stage2a_ongrid_noiseless_20260910_v1` 已完成 120/120 行，用时 71.20 s。固定原两套 5,082 点字典、F1/F2 状态和导频、原估计器及每阶段 2 次局部迭代。原 5 个种子生成的三径就近投到已有网格，复增益保留；每组三径再拆成三个单径。只构造测试样本，不改变生产信道分布或原始结果。15 个单径与 5 个三径共享来源，不是 20 个独立场景。

| 测试 | 协议 | 方法 | 数值恢复 | 原路径容差成功 | 全带 NMSE 均值/dB |
|---|---|---|---:|---:|---:|
| 单径 | F1 | OMP | 15/15 | 15/15 | −180.02 |
| 单径 | F1 | Yang | 15/15 | 15/15 | 数值舍入量级 |
| 单径 | F2 | OMP | 15/15 | 15/15 | −178.42 |
| 单径 | F2 | Yang | 15/15 | 15/15 | 数值舍入量级 |
| 三径 | F1 | OMP | 3/5 | 4/5 | −14.80 |
| 三径 | F1 | Yang | 3/5 | 5/5 | −18.01 |
| 三径 | F2 | OMP | 5/5 | 5/5 | −172.79 |
| 三径 | F2 | Yang | 5/5 | 5/5 | −95.81 |

数值恢复同时要求全部路径误差≤1e-6 m/1e-4°/1e-4°且全带 NMSE≤−80 dB；原容差仍为 3 cm/3°/4°。Oracle 在全部测试中达到数值恢复。独立信道生成与模板前向的最大相对误差为 $6.83\times10^{-30}$。极低 NMSE 只用于验收，不是实际噪声条件下的性能。

逐阶段记录定位到：

- **首次选径正常**：两个协议的全部三径测试中，OMP/Yang 首次均选到真实支持；每条单径独立测试也全部选对。
- **F1 种子 2026091003**：OMP/Yang 第二次都选到错误方位。OMP 方位误差为 2°；Yang 局部细化后仍约 1.98°，全带约 −11.02 dB。该路径单独存在时可恢复，问题出现在多径残差上的候选选择。
- **F1 种子 2026091005**：OMP 第三次选错，最大距离/方位误差为 1 cm/12°；Yang 选择正确并恢复。两者使用同一观测，说明选径规则会影响此处失败。
- **F1 种子 2026091004 的 Yang**：全带 −87.59 dB，最大俯仰余差仅 0.000255°，略超预定严格数值阈值，不应与错误支持混为一谈；仍保留原始 3/5 数值恢复计数。

当前判断：基础单径匹配、复增益和信道重构链路正常；F2 在这 5 个网格内三径样本上能够恢复，因此不能把原失败说成协议必然不可辨识。结合 55→65 dB 几乎没有恢复，离网格失配及其诱发的错误支持是优先排查对象。F1 即使网格内也有多径选径问题，需要区分残差相关筛选和后续局部细化。此次同时移除了噪声与离网格因素，不能单独量化二者各自贡献；第 9–10 节用全因子和 oracle 最近格初始化继续分离了这两部分。

保留的入口与产物：

- `scripts/check_stage2a_ongrid_noiseless.py`：可复现诊断脚本，`--run-id` 指定新目录，默认目录已存在时拒绝覆盖。
- `runs/stage2a_ongrid_noiseless_20260910_v1/config_resolved.json`、`fixtures.json`：完整测试定义、原始与投影坐标、复增益。
- 同目录 `metrics_raw.csv`、`analysis_compact.json`、`analysis_report.md`：逐样本与汇总结果。
- 同目录 `selection_traces.json`、`first_selection_checks.json`：逐径选择、细化前后坐标与残差、首次候选分数。
- 同目录 `code_manifest.json`、`environment.txt`、`status.json`：溯源与完成状态。

历史运行、原配置及估计器文件哈希未变。本次没有重排导频、修改状态、增加迭代或启动正式 100 场景。下一阶段应先隔离原连续真值在无噪声下的离网格恢复与 F1 后续选径问题，再决定是否调整算法、状态或导频；这些调整尚未实施。

## 9. 无噪声离网格 $2^3$ 全因子诊断

运行 `stage2a_offgrid_factorial_20260910_v1` 已完成 640/640 行，用时 298.73 s。距离、方位、俯仰各取“最近已有网格点”或“原连续值”，构成 8 种组合；每种组合仍使用原 5 组三径及其派生的 15 个单径、F1/F2 和 OMP/Yang。全部观测无噪声，原 5,082 点字典、状态、导频、排除半径、估计器及 Yang 的 2 次迭代均不变。几何成功沿用 3 cm/3°/4°；全带 NMSE≤−10 dB 是运行前写入设计文件的描述性筛查，不是正式论文门槛。

单径结果如下。表中“最近格首次胜出”由同协议的粗相关搜索决定，OMP/Yang 相同；“均值”均先在线性 NMSE 域聚合再转 dB。

| 连续维度 | F1 最近格首次胜出 | F1 全带≤−10 dB：OMP/Yang | F1 全带均值：OMP/Yang | F2 最近格首次胜出 | F2 全带≤−10 dB：OMP/Yang | F2 全带均值：OMP/Yang |
|---|---:|---:|---:|---:|---:|---:|
| 无，全部在网格 | 15/15 | 15/15；15/15 | −180.020；−303.211 dB | 15/15 | 15/15；15/15 | −178.421；−303.689 dB |
| 仅距离 | 4/15 | 4/15；4/15 | +1.072；+1.636 dB | 3/15 | 3/15；3/15 | +1.442；+1.822 dB |
| 仅方位 | 3/15 | 2/15；3/15 | +2.046；+1.930 dB | 10/15 | 6/15；10/15 | −1.898；−2.436 dB |
| 仅俯仰 | 15/15 | 15/15；15/15 | −19.018；−80.318 dB | 15/15 | 15/15；15/15 | −19.086；−83.579 dB |
| 距离、方位、俯仰 | 2/15 | 1/15；2/15 | +1.763；+2.164 dB | 1/15 | 1/15；1/15 | +1.766；+2.287 dB |

原连续三径下，F1 的 OMP/Yang 几何成功、全带≤−10 dB 和全部粗支持为最近格均为 0/5，首次选到最近格为 1/5；全带均值为 +1.689/+2.355 dB。F2 对应三项也均为 0/5，首次选最近格为 0/5；全带均值为 +2.473/+3.073 dB。仅距离连续时，多数组合已经失效；仅方位连续时 F1 明显失效，F2 单径较好但三径仍不稳定。仅俯仰连续基本不改变结果。

最近网格模板的归一化观测相关度平方给出同一结论：

| 连续维度 | F1 中位数 / 最小值 | F2 中位数 / 最小值 |
|---|---:|---:|
| 仅距离 | 0.815 / 0.512 | 0.690 / 0.292 |
| 仅方位 | 0.657 / 0.446 | 0.651 / 0.278 |
| 仅俯仰 | 0.996 / 0.965 | 0.996 / 0.965 |
| 三维均连续 | 0.518 / 0.300 | 0.316 / 0.173 |

原样本到最近格的距离偏移中位数/最大值只有 2.438/4.258 mm，但按 30 GHz 带宽计算的跨带相位变化约为 1.53/2.68 rad。方位偏移中位数/最大值为 0.540°/0.969°，在 20° 附近跨 31.778 mm 主轴孔径产生约 1.77/3.18 rad 的空间相位变化。俯仰偏移为 0.525°/1.455°，跨 3.498 mm 短轴只有约 0.20/0.56 rad。该孔径几何解释了为什么当前样本中距离和方位离网格主导、俯仰影响很小。

$c/B\approx1$ cm 是名义时延分辨尺度，不是最近邻稀疏字典的充分采样条件。1 cm 网格允许接近 5 mm 的量化偏移，足以大幅降低全带模板相关度。F2 网格内精确原子的相关度为 1，能够压过约 0.1505 m 的距离伪峰；离网格后最近原子相关度下降，伪峰可反而胜出。F1 中也已观察到 14° 错误模板的相关度高于相邻 20°/22°模板。因而这些结果支持“粗字典基失配诱发错误支持”，不支持“F1/F2 对连续参数必然不可辨识”。

独立信道前向与模板前向的最大 NMSE 为 $6.91\times10^{-30}$。受保护配置、估计器和历史运行哈希均未变化。完整八组合表、配对效应、首次选择和连续三径轨迹保存在运行目录；5 组三径与派生单径存在依赖，不能当作 20 个独立场景。

## 10. 最近网格 oracle 初始化的局部细化检查

运行 `stage2a_nearest_init_20260910_v1` 已完成 40/40 行。它使用原连续真值和无噪声观测，把每条真实路径对应的最近已有网格点直接送入未修改的连续细化函数；状态、导频、上下界、导数步长、每次最大更新和 2 次迭代全部保持生产设置。这一初始化读取真值，只用于检查局部捕获域，不可部署。

| 样本 | 协议 | 几何成功 | 全带≤−10 dB | 全带线性均值转 dB | 逐样本 dB 中位数 |
|---|---|---:|---:|---:|---:|
| 单径 | F1 | 15/15 | 13/15 | −10.090 dB | −52.821 dB |
| 单径 | F2 | 15/15 | 15/15 | −47.248 dB | −54.692 dB |
| 三径 | F1 | 5/5 | 4/5 | +2.205 dB | −43.218 dB |
| 三径 | F2 | 5/5 | 5/5 | −22.350 dB | −25.766 dB |

F2 从最近格初始化后全部通过，说明现有局部细化在正确邻域内能够处理本组连续真值；生产 Yang 的主要失败发生在粗支持获取阶段。F1 也有 13/15 个单径和 4/5 个三径通过，但存在三个未收敛异常：两个单径全带 NMSE 为 +0.475 和 −4.566 dB；种子 2026091005 的三径为 +9.156 dB。最后一个样本的最大距离/方位/俯仰误差为 3.532 mm/0.911°/0.310°，仍满足宽松几何容差，观测残差比例却仅为 0.0041。这说明 F1 压缩观测可被高精度拟合，同时阵元域宽带 CSI 仍错误；全带 CSI 门槛不能由宽松几何容差或观测残差替代。

每次局部细化调用只运行 2 次迭代，每次每维最大移动 0.5 个网格步。它通常能从最近格收敛，但无法可靠挽回相隔约 0.15 m 或多个角度网格的错误粗支持。F1 的单个灾难性样本还表明局部条件或当前迭代预算是次要问题；本轮没有增加迭代，因此不能断言增加迭代一定能修复。网格内 F1 的后续选径错误、较窄的角度排除半径也可能影响三径残差，但单径已经大量失败，故它们不是原连续结果的首要原因。

## 11. 原正 NMSE 的原因层级与下一决策

现有证据支持以下原因顺序：

1. **主要原因是粗字典基失配。** 原连续路径与 1 cm/2°网格之间看似很小的距离和方位偏移，会在 30 GHz 带宽和 31.778 mm 主轴孔径上积累到弧度量级相位误差。最近格原子经常不是粗搜索最高峰，错误支持再进入局部细化，最终产生大于 0 dB 的全带 NMSE。
2. **协议表现不同，但尚不能把状态或导频定为唯一原因。** F2 更容易被约 0.15 m 的梳状距离伪峰取代；F1 更容易命中角度旁瓣。精确网格真值和 oracle 最近格初始化均能恢复，说明两协议至少在这些测试点上存在可恢复解。
3. **F1 有次要的局部与多径问题。** 正确邻域初始化仍有一个三径灾难性异常，网格内后续贪心选径也曾出错。噪声会放大这些问题，但 65 dB 与无噪声结果已经排除“噪声是首要原因”。
4. **全带指标与前向接线没有整体错误。** Oracle、网格内单径、独立前向一致性和 F2 最近格初始化均通过；正 NMSE 是估计结果在阵元—全带域确实比零估计更差，而不是 dB 符号或归一化错误。线性域均值还会被少数灾难性样本显著抬高，因此同时保留逐样本中位数和失败计数。

在 D-023 结束时，正式 100 场景和文献方法仍暂停。当时曾建议新建固定全扇区的 1 mm/0.5°/3°距离—方位—俯仰网格诊断。该网格包含 $651\times41\times7=186{,}837$ 个候选；每协议 1024 维 `complex64` 显式模板约 1.43 GiB。1 mm 距离把最近格最大偏移降至 0.5 mm，对应约 0.314 rad 跨带相位；0.5°方位把最大偏移降至 0.25°，在主轴孔径上约 0.82 rad；俯仰因当前相关度最低仍为 0.965，暂保留 3°。

该固定全扇区提案原计划先看最近细格是否成为单径最高粗相关候选，再看全带 NMSE≤−10 dB。用户随后选择随机波束中心的自适应局部网格，固定全扇区方案没有实施，已由 D-024 和第 12 节替换。

新增入口与产物：

- `scripts/check_stage2a_offgrid_factorial.py` 与 `runs/stage2a_offgrid_factorial_20260910_v1/`：全因子设计、640 行原始结果、八组合汇总、相关度、配对效应与三径逐阶段轨迹。
- `scripts/check_stage2a_nearest_init_refinement.py` 与 `runs/stage2a_nearest_init_20260910_v1/`：oracle 最近格初始化设计、40 行原始结果和局部细化汇总。
- 两个入口默认拒绝覆盖已有运行；需要复现时必须指定新的 `--run-id`。

## 12. 300 GHz 随机波束中心自适应局部网格

用户已确认暂不处理 100 GHz，先完成 300 GHz 代码并自行启动 Monte Carlo。新增配置 `configs/direction1_stage2a_adaptive_300ghz.toml`；载频、带宽、DMA、F1/F2 状态与导频、三种估计器、Yang 的 2 次局部细化、搜索全局边界和评价指标均沿用现有 Stage 2-A。

每个场景先独立生成三个随机粗路径中心，视为观测前已知的三个波束中心；再在中心的自然半功率邻域内连续随机生成真值，保持原三径关系与复增益。该条件用于回答“已进入正确波束邻域后，局部字典能否避开原粗网格伪峰并恢复连续参数”。它没有实现未知场景的全局粗搜索，因此结果只标记为 `diagnostic_only`。真值没有被放到格点或半格点。

自适应网格按实际算子生成：

1. 对每个粗中心、F1 和 F2，沿距离、方位和俯仰分别寻找归一化模板相关度平方 $\rho^2=0.5$ 的最近交点，得到局部 3 dB 宽度。
2. 共同步长取所有局部宽度最小值的四分之一，并分别受 1 mm、0.5°和 3°上限约束。F1/F2 使用同一组候选，避免协议间因网格不同引入额外差异。
3. 每条路径的局部窗总宽为两协议较大 3 dB 全宽的 2 倍；三个窗投到同一全局锚定格点后合并去重。连续真值偏移不超过对应半功率半宽的 0.9 倍，并受 4.5 mm/0.9°/1.2°上限约束。
4. 每个场景保存粗中心、连续偏移、两协议局部宽度、窗边界、实际步长、候选数、最近候选误差和边界截断标记。

配置固定 5 个种子×每种子 20 个场景、55/65 dB、两协议和三种估计器：

$$
100\text{ scenes}\times2\text{ schedules}\times2\text{ SNR}\times3\text{ estimators}=1200\text{ rows}.
$$

运行前已生成全部 100 个预定场景而未执行估计。候选数为 2814–4962，中位数 4050、均值 4064.3，全部真值位于关联局部窗，远低于 50,000 候选上限。实际步长范围为 1 mm、0.323–0.368°和 1.793–3.000°；最近候选最大偏差为 0.500 mm/0.171°/1.498°。共有 16 个局部半宽在既有全局边界处截断，正式运行会保留逐场景标记，不静默外推搜索范围。

为控制逐场景重建字典的成本，CUDA 只用于精确球面模板计算，不用于学习模型。几何和公共传播相位使用 float64，孔径相对相位、累加和存储使用 float32/`complex64`。首个场景自动抽取 16 个模板与 NumPy 参考比较；相对误差必须不大于 $10^{-4}$，且逐模板最小 $\rho^2$ 不小于 0.99999，否则立即停止。当前 RTX 4050 检查中 F1/F2 相对误差分别为 $1.75\times10^{-5}$ 和 $1.31\times10^{-5}$。单场景、两档 SNR、两协议、三方法的 12 行端到端临时检查完整通过，核心运行约 13.65 s；临时产物已清理。

从项目根目录运行：

```powershell
& 'D:\Program Files\Miniconda\Scripts\conda.exe' run --no-capture-output -n ris_td3 python scripts\run_direction1_stage2a.py --config configs\direction1_stage2a_adaptive_300ghz.toml --run-id stage2a_adaptive_300ghz_20260911_v1
```

运行入口默认拒绝覆盖同名目录。该段记录运行前约定：`metrics_raw.csv` 应为 1200 行，`metrics_summary.json` 保存局部网格和计算后端元数据，`analysis/analysis_compact.json` 汇总候选规模、边界截断、全带 NMSE≤−10 dB 比例、三径成功率和局部窗逃逸率。实际结果见第 13 节。

## 13. 自适应局部网格运行结果

`stage2a_adaptive_300ghz_20260911_v1` 已完成 1200/1200 行和 100 个独立条件场景。数据无重复键或必需字段非有限值，全部场景保持方法配对和同观测复用。运行时配置、41 个代码清单文件、结果目录副本与当前对应文件哈希一致。总核心时间为 950.9 s；F1/F2 字典平均每场景构建 0.615/0.360 s，峰值单协议显式字典约 38.77 MiB。

全带结果如下。表中“均值”先在线性域平均再转 dB，“中位数”为逐场景 dB 中位数；达标率使用预声明的全带 NMSE≤−10 dB。

| 协议 | SNR | 方法 | 全带均值 | 全带中位数 | 达标率 | 三径成功率 |
|---|---:|---|---:|---:|---:|---:|
| F1 | 55 dB | OMP | −6.144 dB | −10.867 dB | 57% | 89% |
| F1 | 55 dB | Yang | −6.691 dB | −17.420 dB | 63% | 95% |
| F1 | 55 dB | Oracle | −25.189 dB | −25.788 dB | 100% | 100% |
| F1 | 65 dB | OMP | −6.043 dB | −12.128 dB | 60% | 90% |
| F1 | 65 dB | Yang | −6.754 dB | −28.289 dB | 67% | 97% |
| F1 | 65 dB | Oracle | −35.189 dB | −35.788 dB | 100% | 100% |
| F2 | 55 dB | OMP | −15.101 dB | −15.323 dB | 100% | 99% |
| F2 | 55 dB | Yang | −23.821 dB | −24.039 dB | 100% | 100% |
| F2 | 55 dB | Oracle | −27.597 dB | −28.080 dB | 100% | 100% |
| F2 | 65 dB | OMP | −15.800 dB | −16.039 dB | 100% | 99% |
| F2 | 65 dB | Yang | −33.734 dB | −34.036 dB | 100% | 100% |
| F2 | 65 dB | Oracle | −37.597 dB | −38.080 dB | 100% | 100% |

F2 的导频、全带和未观测域已一致。Yang 在 55 dB 分别为 −23.784/−23.821/−23.827 dB，在 65 dB 为 −33.679/−33.734/−33.743 dB；增加 10 dB SNR 后，全带逐场景改善均值为 9.97 dB。由此可判断：固定粗网格时最近原子相关度下降，才使约 0.15 m 的频域伪峰获胜；在当前额外粗中心先验和局部细采样下，F2 距离混叠不再主导。

F1 仍有稳定长尾。65 dB 下 OMP/Yang 各有 9 个正全带 NMSE 样本；Yang 的 33 个未达 −10 dB 样本中，32 个满足原 3 cm/3°/4°几何容差，只有 7 个离开局部窗并集。55/65 dB 有 28 个 Yang 失败场景重合，8 个相关宽度边界截断场景中只有 3 个失败。失败组和通过组的观测残差比例中位数分别为 0.09238 和 0.09195，残差不能识别该错误。

最差公共场景 `2026091104:1` 给出直接例子：65 dB Yang 的观测 NMSE 为 −23.140 dB、距离和俯仰 RMSE 仅 0.191 mm/0.043°，但方位 RMSE 为 1.680°，全带 NMSE 为 +2.902 dB。两条路径的方位从 23.033°/32.430°被估为 21.102°/34.607°；同一真值的 F2 Yang 方位 RMSE 为 0.011°、全带 NMSE 为 −34.745 dB。这表明 F1 错误来自单配置观测内的局部角度竞争峰或多径歧义，不是当前网格步长、噪声或全局边界。

链路端也呈现相同长尾。65 dB F1 Yang 的波束损失中位数仅 0.0019 dB，但均值、90% 分位和最大值为 1.174/3.204/17.473 dB；F2 Yang 对应为 0.0006/0.0010/0.0041 dB。F1 因一个训练符号和零切换仍有较高平均净谱效率，但尾部失效使当前结果不足以支持正式方法排名或可靠获取结论。

因此 D-024 对 F2 通过，对 F1 只部分通过。当前不应扩大到全扇区密网格：它不会消除 F1 窗内角度歧义。下一步建议保持现有 100 个场景和真值，先做 F1 真值峰—错误峰相关谱及单径/三径分解；再根据结果选择改变 F1 状态、增加少量配置，或把 F1 的目标收缩为直接波束选择。未经新决策不启动正式 Stage 2-A、文献方法或学习模型。

## 14. F1 真值峰—错误峰及单径/三径分解

运行 `stage2a_f1_peak_decomposition_20260915_v1` 复用 D-024 的 100 个连续真值、粗中心和逐场景局部候选，只保留 F1。每个原三径场景按原复增益拆成三个相关单径；观测设为无噪声，用于隔离确定性峰结构。Grid OMP、Yang 的状态、导频、候选、排除盒和两次局部细化均未改变。源运行的 65 dB 三径结果只作为逐场景标签连接，不重新采噪声。

归一化粗相关谱定义为

$$
\rho_i^2(\mathbf y)=\frac{\lvert \mathbf a_i^H\mathbf y\rvert^2}{\lVert\mathbf a_i\rVert_2^2\lVert\mathbf y\rVert_2^2}.
$$

真值邻域沿用估计器的排除盒：距离、方位和俯仰分别不超过 1.5 cm、1°和 1.5°。真值峰—错误峰裕量为邻域内最高 $\rho^2$ 与邻域外最高 $\rho^2$ 之比的 dB 值。该邻域比旧 3 cm/3°/4°成功容差严格，直接检出已知的约 2°角峰错误。

| 方法 | 单径全带≤−10 dB | 三个单径均通过的场景 | 三径全带≤−10 dB | 三径失败且任一单径失败 | 三径失败但三个单径均通过 |
|---|---:|---:|---:|---:|---:|
| Grid OMP | 260/300 | 63/100 | 64/100 | 25 | 11 |
| Yang OG-OLS | 260/300 | 63/100 | 72/100 | 23 | 5 |

单径粗相关最高点有 254/300 位于真值邻域。其余 46 条中，40 条的方位偏差超过 1°，6 条只因俯仰偏差超过 1.5°离开邻域；距离偏差均未超过 0.75 mm。40 条方位错误的粗峰偏差为 1.773–2.193°，且与 OMP、Yang 的 40 条单径全带失败逐条完全重合。Yang 细化后的方位误差仍为 1.807–2.244°，全带 NMSE 集中在 +2.98 至 +3.05 dB，因此两次局部更新没有跨越错误角度盆地。

全部单径的真值峰相对最强邻域外错误峰裕量中位数只有 0.151 dB，10% 分位为 −0.031 dB。40 条错误方位峰中，最强错误峰的 $\rho^2$ 为 0.956–0.995，并比真值邻域最高峰高 0.003–0.183 dB。源 65 dB Yang 失败场景的 99 条派生单径裕量中位数为 0.084 dB，通过场景的 201 条为 0.178 dB；两组仍有明显重叠，峰间裕量是风险信号，不是完美失败分类器。

三径无噪声 Yang 有 28/100 场景未达到 −10 dB，其中 23 个至少包含一条无噪声单径失败，5 个在三个单径都通过时才失败。源 65 dB 的 33 个 Yang 失败场景中，27 个在无噪声三径下仍失败，22 个含无噪声单径失败。最差场景 `2026091104:1` 的无噪声/65 dB 全带 NMSE 为 +2.894/+2.902 dB，几乎不随噪声变化。因而噪声不是主因；单径角度弱区分性主导，多径叠加和贪婪残差选择至少单独对应 5/28 个无噪声 Yang 三径失败。

这一结果不等于连续算子在数学上存在完全相同的两个解。精确真值模板的自相关仍为 1；失败发生在有限候选和当前局部更新下，真值附近原子与约 2°角度旁瓣的差距过小。继续全局加密字典或只增加 Yang 局部迭代不能形成可靠的峰间隔离，且会加入更多竞争原子。下一步应优先做观测状态辨识性筛查：保持 128 导频 RE，比较重新设计的单配置 F1 与少量互补配置控制组，再决定是否保留“单配置完整 CSI”目标。联合选径改进只处理剩余多径部分，不应先于状态筛查。

入口为 `scripts/check_stage2a_f1_peak_decomposition.py`。运行目录保存 600 条单径方法记录、200 条三径方法记录、400 组粗相关峰、逐阶段选择轨迹、最差场景方位相关谱和三张图；原 D-024 运行与源配置哈希未变。300 条单径由 100 个三径场景派生，不能当作 300 个独立统计样本。
